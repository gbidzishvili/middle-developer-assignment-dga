import { ErrorStateDirective } from './error-state.directive';

describe('ErrorStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorStateDirective();
    expect(directive).toBeTruthy();
  });
});
